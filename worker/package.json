{
  "name": "15class-history-worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute 15class-history-db --file=./schema.sql",
    "db:seed": "wrangler d1 execute 15class-history-db --file=./seed.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
